---
command: specflow init
description: Initialize SpecFlow AI in the current project
---

# /specflow:init

Initialize SpecFlow AI project context management.

Creates:
- `.specflow/` state directory
- `docs/spec-flow/` PCB output directory
- Initial project metadata

Usage: `/specflow:init` or `/specflow:init --name "My Project"`
