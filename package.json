{
  "name": "specflow-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "SpecFlow AI — AI-powered project context compiler",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint packages/*/src/ --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18"
  }
}
